@page
@model IniciarSesionModel
@{
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title></title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Raleway&display=swap">
    <link href="css/iniciarsesion.css" rel="stylesheet" />
</head>
<body>


   <div class="w-100 d-flex flex-row align-items-center mb-4">
       <img src="ATOSLogo.png" height="100px" />
       <h1 class="fs-3 d-inline mb-0">AtosLearning</h1>
   </div>


<div class="rectangle mx-auto px-0">
    <p class="crear-cuenta mx-0 text-center mt-2">Ingresar</p>
    <form id="loginForm" method="post" asp-page="/IniciarSesion" class="d-flex flex-column align-items-center">
        <input type="email" placeholder="Correo" name="Email" class="mx-auto w-50"/>
        <input type="password" placeholder="Contraseña" name="Password" id="passwordField" class="mx-auto w-50"/>

        <div class="button-container">
            <button id="signIn" type="button" class="green-button mx-0" onclick="submitForm()">Ingresar</button>

            <button type="button" class="mx-0" onclick="location.href='/CrearCuenta'">Crear una Cuenta</button>
        </div>
    </form>
</div>

<script>
function submitForm() {
    document.getElementById('loginForm').submit();
}
var input = document.getElementById("passwordField");
    input.addEventListener("keypress", function(event) {
      if (event.key === "Enter") {
        event.preventDefault();
        document.getElementById("signIn").click();
      }
    });
</script>

</body>
</html>
